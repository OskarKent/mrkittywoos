import { textFlag } from "https://unpkg.com/cursor-effects@latest/dist/esm.js";
if (window.innerWidth >= 1024) {			
	new textFlag({
		text: "Mr Kitty Woos",
		color: "#00ffff",
		font: "audiowide"
	});
}
